---
export interface Props {
  href?: string;
  type?: 'button' | 'submit' | 'reset';
  disabled?: boolean;
  class?: string;
  target?: '_blank' | '_self' | '_parent' | '_top';
}

const { href, type = 'button', disabled = false, class: className = '', target = '_self' } = Astro.props;
const Element = href ? 'a' : 'button';
---

<Element
  href={href}
  type={!href ? type : undefined}
  disabled={!href ? disabled : undefined}
  target={href ? target : undefined}
  class={`btn-primary ${className}`}
>
  <slot />
</Element>

<style>
  .btn-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 600;
    background-color: #2563eb;
    color: white;
    border: none;
    border-radius: 0.375rem;
    cursor: pointer;
    text-decoration: none;
  }

  .btn-primary:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  /* Responsive PC styles */
  @media (min-width: 768px) {
    .btn-primary {
      padding: 1rem 2rem;
      font-size: 1.125rem;
    }
  }
</style>